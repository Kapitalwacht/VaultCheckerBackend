package com.vaultchecker.platform.credit.domain.model.valueobjects;

/**
 * Whether a pactada interest rate is expressed as a nominal or an effective rate.
 */
public enum RateType {
    NOMINAL,
    EFFECTIVE
}
