package com.vaultchecker.platform;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Entry point for the VaultChecker Platform Spring Boot application.
 */
@SpringBootApplication
public class VaultCheckerPlatformApplication {

	public static void main(String[] args) {
		SpringApplication.run(VaultCheckerPlatformApplication.class, args);
	}

}
